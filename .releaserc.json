{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/github",
    [
      "@semantic-release/exec",
      {
        "publishCmd": "for i in $(seq 1 30); do curl -sf -m 30 -o /dev/null https://jitpack.io/com/github/IUTInfoAix-S201/audio-view/v${nextRelease.version}/audio-view-v${nextRelease.version}.pom && exit 0; sleep 20; done; echo ::warning::Prechauffage JitPack v${nextRelease.version} non servi apres ~10 min - non bloquant, la publication Maven Central se poursuit"
      }
    ]
  ]
}
