{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/github",
    [
      "@semantic-release/exec",
      {
        "publishCmd": "for i in $(seq 1 30); do curl -sf -m 30 -o /dev/null https://jitpack.io/com/github/IUTInfoAix-S201/audio-view/v${nextRelease.version}/audio-view-v${nextRelease.version}.pom && exit 0; sleep 20; done; echo JitPack n a pas servi v${nextRelease.version} apres ~10 min; exit 1"
      }
    ]
  ]
}
